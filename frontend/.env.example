# Copy to .env.local for local overrides (not committed).
# Production: set VITE_API_ORIGIN in Netlify / Vercel site settings → Environment variables (Build).
# Example: VITE_API_ORIGIN=https://your-api.onrender.com

# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_ORIGIN = "https://beatvault-music-streaming-web-application.onrender.com"

PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/music_streaming_app
JWT_SECRET=change_this_secret
# Production frontend (CORS). Optional if the same URL is already baked into server.js defaults.
CLIENT_URL=https://timely-moonbeam-133b32.netlify.app
